스프링부트 백엔드 var 로 되어있는 것 전부 그에 맞는 걸로 바꾸기
예: String, Instance, SecretKey

KPI 0
aggregationType 을 code 이용하는 걸로 바꾸기

지금 실시간 KPI가 가장 최근의 퍼포먼스 1개만을 기준으로 하고 있는데 이걸 여러개 기준으로 바꾸기

KPI-2 직원 상세보기

임시로 Equipment, Process, WorkCenter를 만들어 뒀는데 이걸 정식으로 만들기

isDeleted를 활성화하면 겟으로 못 얻게 하기

isDeleted가 활성화된 테이블과 완전히 같은 테이블이 있으면 이걸 업데이트를 할지 새로 만들지 고민하기
