스프링부트 백엔드 var 로 되어있는 것 전부 그에 맞는 걸로 바꾸기
예: String, Instance, SecretKey

KPI 0
aggregationType 을 code 이용하는 걸로 바꾸기

지금 실시간 KPI가 가장 최근의 퍼포먼스 1개만을 기준으로 하고 있는데 이걸 여러개 기준으로 바꾸기

KPI-2 직원 상세보기

임시로 Equipment, Process, WorkCenter를 만들어 뒀는데 이걸 정식으로 만들기

isDeleted를 활성화하면 겟으로 못 얻게 하기

isDeleted가 활성화된 테이블과 완전히 같은 테이블이 있으면 이걸 업데이트를 할지 새로 만들지 고민하기

N+1 문제 가능성
→ 여기서 shiftAssign 개수만큼 findById가 호출될 수 있어요.
→ employeeRepo.findAllById() 같은 방식으로 한 번에 가져오는 게 더 효율적입니다.

ProcessCert → EmployeeCert 비교
현재는 employeeCertIds.containsAll(entry.getValue()) 로직인데,
이 부분도 DB 레벨에서 처리 가능 (쿼리 최적화).

지금은 DTO 응답이라 괜찮지만, 공정 수가 많아지면 성능 이슈 생길 수 있어요.

Null-safe 리팩토링
process.getDescription() 같은 부분은 null 가능성 있으니 Optional.ofNullable() 처리하면 안전합니다.

DTO 변환 책임 분리
EquipDetailDto 변환도 Service에서 직접 하지 않고, 별도의 Mapper 클래스로 빼면 더 명확해져요.

예: EquipDetailMapper.toDto(equipment, workOrder, shift, …)


==프로세스를 새로 생성할때 같은 아이디의 deleted가 true일때 false로 업데이트 하게 하기


시프트 다시 만들기
기본적으로 설비 목록을 보여주고 날짜를 설정해서 설비 목록의 캘린더 작성 버튼을 누르면 캘린더가 자동 작성 되도록 변경

시프트 캘린더 목록을 보여줄때 현재 해당 시프트의 작업자 이름과 작업 인원을 보여주도록 함 그리고 배치 버튼을 만들어서 그걸 누르면 직원을 배치할 수 있도록 하기

시프트 캘린더 목록에 배치된 직원 상세보기가 가능하게 하기
